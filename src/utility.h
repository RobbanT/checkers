//Ändrar bara namnet på unsigned short till u_short då jag tycker att unsigned short är ett för långt namn på en datatyp.
typedef unsigned short u_short;

//Skapar denna enum för att kunna använda mig av booleska värden.
typedef enum{
	FALSE, 
	TRUE
}bool;